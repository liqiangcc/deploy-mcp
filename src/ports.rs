//! Interfaces owned by the application core.
//!
//! `RemoteExecutionPort` and `DeploymentRepository` will live here so concrete
//! adapters remain replaceable and dependency direction stays inward.
